#!/usr/bin/env bash
if ! gem list foreman -i --silent > /dev/null; then
  echo "Installing foreman..."
  gem install foreman
fi

docker-compose up -d

./bin/rails db:migrate

exec foreman start -f Procfile.dev "$@"
